services:
  # Backend API Service
  - type: web
    name: shoaib-portfolio-api
    env: python
    plan: free
    buildCommand: ./build.sh
    startCommand: gunicorn --bind 0.0.0.0:$PORT app:app
    envVars:
      - key: FLASK_ENV
        value: production
      - key: MONGODB_URI
        sync: false
      - key: SECRET_KEY
        generateValue: true
      - key: MAIL_USERNAME
        sync: false
      - key: MAIL_PASSWORD
        sync: false

  # Frontend Static Site
  - type: web
    name: shoaib-portfolio-web
    env: static
    buildCommand: cd frontend && npm ci && npm run build
    staticPublishPath: ./frontend/build
    envVars:
      - key: REACT_APP_API_URL
        value: https://shoaib-portfolio-api-1k6v.onrender.com
      - key: NODE_ENV
        value: production
      - key: GENERATE_SOURCEMAP
        value: "false"
